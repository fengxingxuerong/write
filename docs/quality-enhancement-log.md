# 墨匠质量增强包 — 改动清单

> 记录一次系统性质量优化（2026-09），目标：成书质量对标番茄签约/上架标准。
> 改动覆盖生成侧、质检侧、质量闭环、工具链，Dart 与 Python 双端对齐。

---

## 一、仓库卫生（安全）

| 文件 | 改动 |
|---|---|
| `.gitignore` | 新增忽略：`data/`（大体积语料/产物）、`__pycache__/`、`*.py[cod]`、`.env`、`.env.*`、`*.key` |

⚠️ 遗留：`scripts/generate_novel.py` 第 8 行附近仍硬编码 AMD API Key，建议轮换并改环境变量注入。

---

## 二、生成侧（怎么写）

| 文件 | 改动 |
|---|---|
| `lib/engine/writing_guidelines.dart` | ① 新增「网文商业结构」块：黄金三章 / 爽点优先 / 章末钩子强制 / 追读欲；② 核心技法新增第 7 条「变强具象化」（含蓄爽点：丹田温热/掌心发烫式身体异动，直白"突破"限 1 次） |
| `lib/ai_pipeline/prompts/pipeline_prompts.dart` | 强化 5 处：写手系统准则（商业结构+变强具象化）、规划官（黄金三章链/每章爽点+钩子）、场景规划（爽点场景/末场景「合」/第一章快速触发）、场景正文（末场景强制钩子）、编辑（保留并强化章末钩子）；新增 `qualityReviewPrompt`（五维评分）、`rewritePrompt`（低分定向重写）、`stateExtractPrompt`（跨章状态提取）；`scenePlanningPrompt`/`scenePrompt` 加 `state` 参数 |
| `lib/engine/multipass/scene_builder.dart` | 场景规划加爽点/钩子要求；兜底骨架末场景钩子强化 |
| `lib/engine/multipass/multi_pass_chapter_engine.dart` | 末场景强制「章末钩子」指令 |
| `scripts/generate_novel.py` | SYSTEM_PROMPT 商业结构+变强具象化；规划/场景/写手/编辑 prompt 同步；`scene_planning_prompt`/`scene_prompt` 加 `state` 参数 |

## 三、质检侧（怎么查）

| 文件 | 能力 |
|---|---|
| `lib/ai_pipeline/services/pipeline_qa.dart` | ① 章末钩子检测（直白+隐喻双通道，词表经 33 章实测校准，命中率 82%）；② 开场节奏检测（强/弱双级信号，修复"一碰就碎"式比喻误报）；③ 爽点双通道：直白爽点 💥 + 变强异动 ✨（含蓄变强流不再误判"爽点过淡"）；④ AI 味深度检测（句长变异系数 CV / 「的」字密度 / 叠词修饰 / 句首连接词，统计层反 AI 腔） |
| `scripts/generate_novel.py` | 同源同步：`HOOK_WORDS`、`THRILL_WORDS`、`POWER_SURGE_WORDS`、`deep_ai_metrics`、`has_quick_opening`（双级）；质检汇总打印 🪝/⚡/💥/✨/🤖 |

**词表校准历程（真实成书实测驱动）**
- 钩子词表：初版 39% 命中 → 补隐喻式钩子（监视/诡谲意象/悬而未决）→ 82%
- 爽点词表：直白词仅命中 5 处 → 发现含蓄文风爽点藏在身体异动（58 处）→ 新增变强异动通道
- 开场词表：修复单字词比喻误报 → 强/弱双级判定

## 四、质量闭环（怎么改）

| 能力 | 位置 | 说明 |
|---|---|---|
| 语义五维评分 | `ai_pipeline_service.dart` + `novel_pipeline.py` | 审校官每 3 章评分开篇/爽点/钩子/动机/节奏，`<60` 标记告警 |
| 低分自动重写 | 同上 | 评分 `<55` 自动触发编辑官定向重写（保留情节/钩子，针对薄弱维度），失败保留原文 |
| 跨章状态追踪 | 同上 + models | 每章提取人物伤势/修为/物品/承诺状态清单，注入下一章 prompt 防穿帮；断点续传自动恢复 |

配置字段（`AiPipelineConfig`，序列化向后兼容）：`useQualityReview`/`qualityReviewEvery`/`autoRewriteLowScore`/`rewriteThreshold`/`useStateTrack`

## 五、工具链（怎么判断）

| 工具 | 能力 |
|---|---|
| `scripts/qa_scan_existing.py` | 单本扫描：逐章指标（钩子/开场/爽点/异动/AI味/AI深度）+ 爽点密度曲线 + 节奏塌陷检测 + 黄金三章专项体检（变故/落差/金手指/目标/钩子）+ 签约可行性报告（五维 100 分制 + 对标番茄要素） |
| `scripts/ab_compare.py` | A/B 对比：两本成书并排（新旧标准/双模型/校对前后），自动判定胜负与显著差异 |
| `scripts/run_quality_test.ps1` | 一键实测：新版生成 3000 字 + 基线扫描连跑 |

## 六、流水线运行效果示例

```
  [规划] 5 场景：起/承/转/转/合
  [评分] 第 3 章 综合 82 分（开篇85/爽点60/钩子90/动机75/节奏80）节奏紧凑
  [评分] 第 6 章 综合 47 分 过渡章注水严重
  [重写] 第 6 章 47 分 < 55，触发自动重写...  2876 字 -> 3012 字
  [状态] 已更新跨章状态清单（5 行）
  [质检] ⚠ 第 6 章 AI 腔偏重：句长过于均匀（CV=0.49）
  [完成] 第 6 章：3012 字 | 累计 15678 字
```

## 七、验证结论

- 全部检测器经《碎脉铸仙录》10.6 万字真实文本逐项验证：钩子 82% 命中、含蓄变强流正确识别、黄金三章体检能发现真实短板（第 2 章目标偏弱）、AI 深度判定与真实文风一致（且修正了人工直觉的两次误判）
- 词表校准方法论：真实成书回测 → 发现漏检/误报 → 修正 → 双端同步 → 复测

## 八、2026-09-12 LLM 角色矩阵 v2（8 角色 9 岗位重新分配）

### 背景
用户提供 9/12 端点资源清单（AMD / 商汤 3 key / OpenRouter / NVIDIA），两轮大 token 探针（max_tokens=300/2000）全量实测后重排角色。探针脚本：`scripts/probe_roster_20260912.py`、`probe_roster_r2_20260912.py`；冒烟：`scripts/smoke_roles_20260912.py`（9/9 通过）。

### 探针关键发现
- 商汤 deepseek-v4-flash 三 key 复活（上轮 401）：3-4s 快、思维链轻，但思考也会随机吃 400-6000+ token（随机性死循环）
- OpenRouter：账户 0 余额（402）；stealth/ox-alpha 内测结束，官方提示其即 z-ai/glm-5.3-flash；充值后可解锁 glm-5.3 / v4.1-flash / kimi-k3
- NVIDIA：z-ai/glm-5.2 确认 410 EOL（2026-08-21 下架）；新增可用 deepseek-ai/deepseek-v4-flash-0731（3.1s）；kimi/pro 在 NV 超时不可用
- kimi-k3 硬约束：temperature 仅允许 1（传 0.8 直接 HTTP 400）
- 挖出 3 处静默失效 bug：pro 角色调用点 max_tokens 覆盖（1000/800/200）低于思考链消耗（2000-2900），致语义评分/跨章状态提取/爽点诊断长期空输出

### 角色分配 v2
| 角色 | 主 | 备 |
|---|---|---|
| 规划官 | glm-5.2 K1→K2→K3 | NVIDIA dsf-flash → AMD |
| 写手 | 商汤 dsf-flash K1（根治 glm 场景死循环） | glm-5.2 K1 → AMD → NVIDIA |
| 编辑 | kimi-k3 K2（temp=1 守卫） | glm-5.2 K3 → AMD |
| 标题官 | 商汤 dsf-flash K1（max_tokens 800） | 回退章纲标题 |
| 审校 | deepseek-v4-pro K1（6000 token） | glm-5.2 K3（改 VERIFIER_CHAIN） |
| 状态提取官 | 商汤 dsf-flash K3（轻任务换轻模型） | 失败保留旧状态 |
| 伏笔官 | 商汤 dsf-flash K2（1500 token） | 失败保留旧台账 |
| 世界观守护官 | deepseek-v4-pro K2（6000 token） | — |
| 爽点总监 | 商汤 dsf-flash K2（1000 token） | — |

### 代码改动（均在 scripts/novel_pipeline.py）
1. 三链五字典重构 + setup_keys 索引同步；VERIFIER 字典升级为 VERIFIER_CHAIN 故障转移链
2. `llm_call` 新增 kimi 温度守卫（kimi 一律强制 temperature=1）
3. 新增 STATE_EXTRACTOR 角色；THILL/伏笔/状态提取由 pro/glm 换 dsf-flash
4. 修复 `--skip-amd` 的 AMD 链位索引（PLANNER_CHAIN[4]/WRITER_CHAIN[2]/EDITOR_CHAIN[2]）
5. 健康池补盲：HTTP 200 但思考链吃满致正文空 → 计入 _mark_fail（3 次冷却 5 分钟），链路自动避开死循环端点
6. 写手链 AMD 排 NVIDIA 前（30s 稳定优于 300s 超时风险），最坏路径 352s → 约 60s

### 验证
- 冒烟 9/9 通过（规划 16.8s / 写手 12.3s / 编辑 7.1s / 标题 2.7s / 审校 13s / 状态 4.9s / 伏笔 4.2s / 爽点 2.3s / 守护 6.5s）
- py_compile 通过；写手思考死循环随机性复现并验证健康池计数路径

### 补充（同日）：第 10 角色「终审官」补设

- 空缺：此前终局把关只有本地规则评分（签约可行性报告/评估卡），LLM 侧无通读级总评，节奏曲线/人物弧光/伏笔回收质量/结局力度无人把守
- 新增 `CHIEF_EDITOR_CHAIN`（pro K2 主 → glm K1 备，max_tokens 6000-8000）与 `chief_editor_prompt`
- 输入：全书结构化摘要（逐章指标表 + 跨章状态 + 未收伏笔台账）+ 三段样本正文（首章/末章/评分最低章），避免整本超长输入
- 输出：JSON 总评（总分/六维/top_issues 带指标证据/fix_priority 可执行建议/商业对标）
- 挂载：Phase 3.5（本地质检后、导出前）；产物 `<output>.终审卡.txt` 与评估卡并列，同时写入 jsonl `type=chief_review`
- 开关：`--no-chief-review` 可跳过（默认开启）
- 冒烟：3 章迷你书实测 94.3s 返回，准确命中"节奏下探章/未收伏笔/金手指无代价"三类通读级问题，问题均引用逐章指标为证

### 补充（同日）：终审官打回重写权

- 机制：终审总分 < 打回线（默认 75，`--chief-rewrite-threshold` 可调）→ 从 top_issues 提取被点名章节 → EDITOR_CHAIN 定向重写（最多 `--chief-rewrite-max` 章，默认 3）→ 该章重新评审并同步 reviews → 终审官二审一轮，以第二轮为准
- prompt：`chief_rewrite_prompt`（问题+证据+修改方向注入，只修问题不动剧情，反AI腔，字数只多不少）
- 续传安全：重写内容以 `type=chief_rewrite` 补丁记录落盘，`apply_chief_rewrites` 在续传时 last-wins 回放（不经 type=chapter，避免 load_state 重复章）；补丁含 has_hook/words 完整还原章记录
- 附带修复：续传时 `load_reviews_from_jsonl` 重建评审记录（每章 last-wins），修复断点续跑后评估卡"章节：0/均分 0.0"问题
- 开关：`--no-chief-rewrite` 关闭打回权；`--no-chief-review` 关闭整个终审
- 验证：4 项续传回放单测 PASS（补丁回放/reviews 恢复/字段还原/多次补丁 last-wins）；真实 LLM 闭环冒烟通过（72 分触发打回→命中第 2 章重写→二审 65 分，全程两处 429 被故障转移链自动吸收，278s）

## 九、2026-09-12（晚）第 11 角色「大纲终审官」写前守门（结构打回权前置版）

### 背景

终审官《断脉逆命诀》二审暴露的能力边界：其打回重写权仅限单章层级，二审抓出的三个结构级问题
（第 1 章 6 条伏笔全悬、末章停"大典前夕"无结局、单章重写后 65→66 几乎不涨）根子全在上游大纲，
修章治标不治本。与其等收官才发现（已烧完全书配额），不如在写第 1 章之前就守门——
大纲是全书唯一的低频高价值产物，守门成本一次性、收益全书级。

### 新角色：大纲终审官（OUTLINE_REVIEWER）

- **挂载点**：Phase 1.5——规划官出大纲 + concretize_world 之后、第 1 章动笔之前；`--no-outline-review` 可跳过（默认开启）
- **链路**：`OUTLINE_REVIEWER_CHAIN`＝商汤 deepseek-v4-pro K2 主 → glm-5.2 K1 备（max_tokens 8000：pro 思考链 2000-2900 + 修订版大纲全量 JSON）
- **检查清单**（由真实成书教训沉淀成硬规则）：①伏笔分布——单章新埋 ≤2 条、第 1 章限 1-2 条核心伏笔、总数 ≤ 章节数×0.8、每条须有回收安排；②末章必须有结局——禁"前夕态"收尾；③单章信息密度——新设定 ≤2 个/章；④钩子链——每章有钩子且类型不连续重复；⑤主线闭环——动机-目标-障碍-代价 + 金手指带代价；⑥节奏——连续 2 章纯铺垫算塌陷
- **输出**：JSON（pass/overall/issues 带大纲证据/advice/revised_outline）；pass=false 时修订版全量大纲必填
- **守门逻辑**：pass 放行；fail 且修订版有效 → 顶层字段兜底合并（title/blurb/tags/protagonist/world/hook 从原大纲补齐）→ 再过一次 concretize_world（幂等）→ 采纳修订版；修订版缺失/无效则放行原大纲（问题清单留痕）
- **LLM 全挂/解析失败**：放行原大纲不阻塞生成（守门岗不得挡住整本书，与项目兜底原则一致）

### 续传安全设计

- 原始大纲先落盘（type=outline，崩溃恢复依据）；修订版作为**新一条 type=outline 追加**——
  `load_state` 按 last-wins 读取，续传天然取修订版；续跑时 outline 已存在、Phase 1 整块跳过，不会重复守门
- 审查过程落 `type=outline_review` 记录留痕（pass/overall/issues/adopted），load_state 不读该类型，不影响任何现有逻辑

### 验证

- `py_compile` 通过
- 符号冒烟 4 项：链路结构（pro 主/glm 备/8000 token）✅、setup_keys 覆盖新链 ✅、
  outline_review_prompt 生成（检查清单/大纲全文/JSON schema 齐全）✅、`--no-outline-review` 被 argparse 正常接受 ✅
- 冒烟过程中流水线无 key 全链路 401 退出，顺带验证新代码未破坏现有规划链路/健康池/故障转移
- 待真书验证：下一本新书跑 Phase 1 时观察守门出分与修订采纳（建议用 6 章小样书验证后入库）

### 备注：本次改动同时回应架构评审报告（2026-09-12）的两个结论

- 「大纲层无人守门是最大盲区」→ 本岗即"结构打回权"的写前前置版，收官期结构打回权（反喂规划官加章）仍按原计划推进
- 「规划官选型错位」→ 未直接更换规划官主位（避免动已验证链路），但守门岗用 pro 补上了"最强模型守最重要产物"的空位；规划官主位是否换 pro/glm-5.3 留待用户拍板（涉及配额消耗变化）

## 十、2026-09-12（晚）第 12 角色「读者官」+ 评审-质检对齐

### 背景

架构评审报告指出两个盲区：①12 角色全是生产侧，没有消费侧视角——终审官抓到的「第 5 章
评审 100 分但钩子无」正是生产侧自嗨的产物（本地评审器的评分体系里根本没有钩子项，
`review_chapter` 从不知道钩子检测结果）；②番茄逻辑的核心是追读，缺一个模拟读者的角色。

### 新角色：读者官（READER_PROXY）

- **链路**：商汤 dsf-flash K3（与状态提取官同 key 分摊轻任务），max_tokens 500，每章 1 次
- **挂载点**：每章末尾 7.7 步（状态/伏笔提取后、本章完成前）——读本章最终正文，以追读读者
  身份答三问：追不追（追/犹豫/弃+原因）/ 哪里想划走 / 下一章最想看到什么
- **输出**：纯文本 3 行（非 JSON，轻任务避免解析失败，与状态提取同理）
- **注入**：反馈存 `reader_hint`，下一章场景规划时拼入 state_inject（「读者试读反馈——
  规划本章时尽量回应其期待、避开其弃点」），与跨章状态、未收伏笔摘要同通道
- **续传**：`type=reader_feedback` 落盘 jsonl，`load_reader_feedback` 取最后一条恢复
- **开关**：`--no-reader-proxy`（默认开启；失败空过不阻塞，下一章无注入）

### 评审-质检对齐（治「100 分无钩子」）

1. **本地评审器钩子红线**（`fanqie_review.py`）：`review_chapter` 新增可选参数 `has_hook`
   （None=未检测，行为不变，CLI/旧调用完全兼容）；False=明确无钩子 → 记「重写」级问题
   扣 8 分，进 problems 与 fix_prompt 定点修链路。novel_pipeline 三处调用
   （主评审/定点修复评/首屏强化复评）全部传入 `has_ending_hook(对应文本)`
2. **LLM 审校证据注入**：`quality_review_prompt` 新增可选参数 `qa_evidence`——章级评分
   （每 3 章）时注入本地质检证据（钩子命中/直白爽点/变强异动每千字），prompt 明确要求
   「评分必须与证据对照，不得矛盾」。默认空参，旧调用行为不变
   - （注：Dart 侧已完成双端同步——2026-09-23 `ai_pipeline_service.dart` 调用点组装 qaEv 传入 qualityReviewPrompt，替身测试断言评分 prompt 携带证据块）

### 验证

- `py_compile` 两文件通过
- 符号冒烟 4 项全过：①读者官链路+三问 prompt ②评分 prompt 兼容旧调用+证据注入正确
  ③评审器钩子红线（同文本：有钩 68 分 / 无钩 60 分，出「重写」级问题；未检测路径不变）
  ④reader_feedback 续传恢复 last-wins 正确
- 待真书验证：下一本新书跑 Phase 2 观察读者反馈质量与注入效果

## 十一、2026-09-12（晚）句式层 AI 指纹检测 + 写手约束补强

### 背景

架构评审发现三类「句式层 AI 风格指纹」：高密度比喻、单句成段频率过于规律、身体反应四件套
（发烫/发凉/嗓子发干/汗毛立起）——词表检测抓不到（不是词的问题）、CV 句长指标也抓不到
（句式模式问题），是反 AI 腔体系的盲区。另发现关键缺口：FANQIE_SYSTEM_PROMPT 第 21 条
其实**早已写明**比喻限额（「像……似的」每千字不超过 2 次），但质检侧完全无法验证——
「有约束、无检测」，写手违规也无法进定点修链路。本轮补齐检测侧。

### deep_ai_metrics 新增句式指纹三项（generate_novel.py）

- `metaphor_density` 比喻密度/千字：明喻强结构正则（像X一样/似的/般、跟X似的、如同X一般）+ 比喻独词（仿佛/宛如/好似/犹如/恰似）；「他像他爹」类判断句不带结构标记不计入（宁漏检勿误报）
- `single_para_rate` 单句成段占比：≤14 字短段 / 非空段落
- `body_reaction_density` 身体反应密度/千字：19 个身体反应词（发烫/发凉/嗓子发干/汗毛/头皮发麻/牙根发酸/太阳穴一跳等）
- **level 并入（保守设计）**：三项中 ≥2 项超标 → level+1（不逐项累加），level 范围 0-4 → 0-5，判定阈值 ≥3 不变；旧消费方只取 `["level"]`，新增 key 向后兼容

### 阈值回测校准（四本真实成书）

| 书 | 比喻/千字 | 单句段% | 身体反应/千字 |
|---|---|---|---|
| 碎脉铸仙录（旧基线） | 1.98 | 18.3 | 0.46 |
| 断脉逆命诀（新版） | 1.22（第1章 3.66） | **31.0** ❌（第5章 44.7） | 0.71（第1章 1.53） |
| 铁幕孤刃（人工确认能打） | **4.75** ❌ | 11.3 | 1.15 |
| 铁掌破风（机器虚高） | 1.01 | 11.7 | 0.58 |

定线依据：比喻 2.0（对齐 prompt 第 21 条承诺线）；单句段 30%（正常书 11-18%，仅断脉超标——
其第 5 章恰为终审官点名的追读断裂章，指标抓到真问题）；身体反应 1.5（回测后从 2.5 收紧，
正常书 0.5-1.2，仅风格指纹最重的断脉第 1 章 1.53 超线）。
**注意**：《铁幕孤刃》比喻 4.75 超线但人工确认为好书——句式指纹线定位是「标记风格特征
供人工复核」（与 fanqie_review 红线词「提示」级同理），单凭比喻超标不会拉 level。

### 写手约束补强（fanqie_prompts.py）

- 第 20 条扩展：身体反应类描写每章合计 ≤2 处、同一场景最多 1 处，情绪优先动作与物件外化
- 第 21 条扩展：禁止相邻段落用同一结构的比喻
- 第 22 条扩展：单句成段（喘气段）每章 ≤4 处、禁连续两段单句段

### 验证

- `py_compile` 四文件通过
- 冒烟 4 项：空文本安全 ／ 高指纹文本三项全超且 level=原四项2+指纹1=3 ／ 正常书（《铁掌破风》）三项全不超 level=0 无误报 ／《断脉》第 1 章 fp_over=3 正确捕获
- **双端同步（Dart 侧）已完成（同日晚间续）**：
  - `lib/ai_pipeline/services/pipeline_qa.dart`：新增 `_metaphorPattern`（RegExp 对齐 METAPHOR_PAT）、
    `_bodyReactionWords`（19 词对齐）、`_singleParaMaxChars`、`styleFpLimits`（对齐 STYLE_FP_LIMITS）；
    新增 `styleFingerprintMetrics()`；`deepAiMetrics` 三项并入（fpOver>=2 → level+1，0-4→0-5）；
    `deepAiIssues` 补三条指纹超标告警；类注释双端同步清单更新（补 METAPHOR_PAT / BODY_REACTION_WORDS / STYLE_FP_LIMITS）
  - `lib/engine/writing_guidelines.dart`：antiAiTone 补三条（身体反应每章≤2 处/同场景≤1 处、
    禁相邻同构比喻、单句段每章≤4 处且不连续），与 fanqie_prompts.py 第 20/21/22 条对齐
  - `lib/ai_pipeline/prompts/pipeline_prompts.dart`：`qualityReviewPrompt` 加可选命名参数
    `{String qaEvidence = ''}`（与 Python qa_evidence 同口径），旧调用点零改动
  - 验证：`flutter analyze` 通过——本次改动引入的 4 个 error 已修复归零
    （初版 `<String>[bool表达式]` 类型笔误 → `<bool>[...]`）；
    剩余 1 个 error（`book_qa_report_dialog.dart` ToolButton 未定义）为改动前既有问题，与本次无关
  - 消费方兼容确认：`composite_quality_gate.dart` 只取 `deep['level']`，`chapterReport` 的
    needsPolish 阈值 level>=3 语义不变，新增 key 向后兼容

### 补充（2026-09-13）：终审官「结构打回权」v1 实现与真书验证

- 新增 `structure_fix_prompt`（规划官裁决是否补收尾章，裁决须点名回收哪几条伏笔）+ `generate_appended_chapter`（收尾章走完整角色链：场景规划→写手→编辑→标题→番茄评审门→状态/伏笔提取）
- 挂载：Phase 3.5 二审后、终审卡前；`--no-chief-structure` 关闭，`--chief-structure-max` 默认 2 章
- 续传安全：新章以 type=chapter 落盘 + 新大纲以 type=outline 追加（load_state 取最后一条，续跑无重复章）
- 解析鲁棒性：规划官裁决 JSON 严格+修复双模式，失败自动重试一次；**修复"解析失败被误报成判定无需加章"**（driver 与 main 两处同步）
- 真书验证《断脉逆命诀》：一审 71 → 规划官裁决补章 → 第 7 章《玉裂藏珠》3292 字（评审 84 达线）→ 三审 64 分，终审卡轨迹完整
- **暴露新问题-指令执行衰减**：规划官明确要求"回收至少 2 条核心伏笔"，写手产出却是开脉突破悬念，6 条伏笔台账仍全 open——场景规划与正文未兑现回收指令，待下轮强化「回收清单逐条打勾」机制
- **暴露新问题-终审评分方差**：同一本书跨轮终审 83/68/64/71/64 波动，固定打回线噪声大，待引入多采样均值或区间判档
- 代码量：+约 230 行（novel_pipeline.py），驱动脚本 scripts/run_structure_fix_20260913.py（含 env STRUCTURE_THRESHOLD 覆盖打回线用于强制触发验证）

### 补充（2026-09-13）：回收清单打勾机制（治指令执行衰减）v1

- 问题：结构打回验证中，规划官下令"回收至少 2 条核心伏笔"，写手产出却是开脉突破悬念，台账 6 条仍全 open——泛化的"回收伏笔"指令在写手端衰减
- 方案（三端注入+双轮校验）：
  1. `build_promise_block`：从伏笔台账提取 open 条目 → 构造成「必兑现清单」硬约束（每条明确要求答案以对话/物证/内心揭示落到纸面，不得只暗示）
  2. 注入：清单进入场景规划 state + 写手 scene_prompt（write_state），收尾章替代原软提示
  3. `verify_and_repair_promises`：伏笔官（dsf-flash）逐条打勾校验（✅/❌+证据句）→ 未兑现条目由编辑链定向补写一轮（只加不改、答案落纸面）→ 复验
- 持久化：章节记录 issues 记"必兑现清单 n/m 条兑现" + jsonl type=promise_check 独立记录
- 单测 3 项 PASS（closed 过滤/空台账/校验 prompt 结构）；真书验证待配额恢复后执行
- 代码位置：scripts/novel_pipeline.py `build_promise_block` / `verify_promises_prompt` / `verify_and_repair_promises`，收尾章流水线集成

### 补充（2026-09-13）：全链路测试《晏无咎·碎脉之辱》（fulltest_20260913）

- 5 章 12104 字，覆盖：写前守门（大纲终审官）→ 逐章 10 角色 → 第 5 章守护/爽点路径 → 本地质检 → 终审 76 分（压线免打回）→ 评估卡/终审卡/上架包
- 429 换链战绩：商汤 429×44 + 超时×7 + 冷却×38 = 89 次韧性事件，人工干预 0 次——「429 就换下一个」完全体验证
- 定点修救章：ch2 60→76、ch3 76→84、ch4 60→92（抽读确认真人级笔感：具象触觉区分、灰烬"血"字钩）
- 终审官揪出真问题：第 2-3 章连续无章末钩子（追读命脉）、末章截断
- 新发现缺陷三项（已录入交付文档已知问题）：①编辑链过度压缩（3805→1921 砍半）②末章正文截断 ③终审评分方差（76 vs 打回线 75 边缘判定）

### 补充（2026-09-13）：编辑链字数下限守卫（EDITOR_MIN_RATIO）

- 问题：fulltest 实测编辑链把 3805 字章润色成 1921 字砍半且被无条件采纳（主循环编辑采纳原本无任何字数守卫），违反"字数只多不少"约定
- 修复：新增常量 `EDITOR_MIN_RATIO = 0.85`，统一覆盖 5 处采纳点——①主循环润色 ②结构补章编辑 ③结构补章定点修 ④打勾补写 ⑤主循环定点修 ⑥终审打回重写；过度压缩视为无效产出，保留原文并打印告警（含百分比）
- 根因定位：主循环编辑采纳原为 `if edited: final_text = edited`（非空即采纳）
- 回归验证（fulltest 真实数据）：第 4 章案例 3805→1921（50%）会被拒 ✅；正常微缩 95% 放行 ✅；**全书 5 章中 4 章会被拦（67%/54%/66%/79%）——证明编辑压缩是系统性异常而非偶发**，仅第 1 章 96% 正常，说明编辑链具备正确行为能力、压缩属模型异常输出，守卫策略正确
- 残留检查：旧阈值 0.5/0.6/0.8 全部清除，py_compile 通过
- Dart 端 lib/ai_pipeline/ 同步待办（注释已标注）

### 补充（2026-09-13）：终审评分方差治理（多采样中位 + 区间判档 + 硬伤告警）

- 问题：终审单轮评分跨轮方差大（同书实测 83/68/64/71/64），fulltest 76 vs 打回线 75 的边缘判定暴露噪声风险；且曾出现"总分 83 但伏笔回收仅 30"的硬伤被总分掩盖
- 修复三件套：
  1. **多采样中位**：`_chief_pass` 每轮采样 `--chief-samples`（默认 2）次，overall 与六维各取中位数合并（`_merge_chief_samples`）；解析失败采样自动跳过，全失败才报错
  2. **区间判档**：`verdict_from_score` 本地按分数判档（≥80 高潜可投 / 70-79 可投 / 60-69 需打磨 / <60 不建议），同分必同档；模型判词降级为 `model_verdict` 参考字段
  3. **硬伤维度告警**：任一六维 ≤40 打印 ☠ 告警写入终审日志，不因总分过线而漏报
- 成本：一审从 1 次 pro 调用变 2 次（每本 +1 次调用，约 +90s），换判定可信度
- 单测 4 项 PASS：区间判档 / 双采样中位合并（83/68→76）/ 硬伤形态 / 全失败安全返回；py_compile 通过
- 代码位置：scripts/novel_pipeline.py `verdict_from_score` / `_merge_chief_samples` / `_chief_pass` 改造 / `--chief-samples`

### 补充（2026-09-13）：回收清单打勾机制真书验证完成（恶劣环境下）

- 验证环境：商汤 tpm 风暴 + glm/dsf 思考链风暴 + AMD 502 全线恶劣——在最坏环境下完成验证，韧性含金量高
- **打勾机制首次完整跑通**（第 10 章《裂印惊变》）：6 条必兑现清单 → 5✅/1❌ 逐条校验，证据句全部来自真实正文（"赵乾，你钉我三年""三年前我灵脉断裂那晚，你在场"）；❌ 条目定向补写失败后如实记录（零静默）
- 对比：第一版第 7 章 0/6 兑现却拿 84 分假象 → 第 10 章 5/6 兑现 + 逐条证据 + 未兑现如实标红——机制价值实锤
- 过程中加固四项：①打勾/台账提取 max_tokens 1500/2000→4000（思考链吃满截断）②校验失败重试+☠显式告警+失败标记落盘（堵静默跳过）③校验改走故障转移链（复用编辑链，单点 FORESHADOWER 风暴期原地连败）④终审采样改「目标成功数」补打（最多 2× 次数，两连败不再放弃）
- 全书现状：11 章 41772 字，三审合并 63 分（多采样中位），五条问题全部具体到章（第 8 章 6552 字超长两倍/第 10 章高潮后无钩子/第 1 章伏笔超载等）
- **新发现待办：打勾✅未联动台账**——第 10 章打勾 5 条 ✅ 但台账仍 open 6/closed 0（伏笔官独立提取在限流下多次失败未标记 closed）。下一步：打勾 ✅ 条目直接联动台账 closed（校验证据即回收凭证），不再依赖伏笔官二次提取

### 补充（2026-09-13）：打勾 ✅ 联动台账 closed（闭环焊死）

- 问题：第 10 章打勾 5 条 ✅ 但台账仍 open 6/closed 0——台账更新依赖伏笔官二次提取，限流/思考链风暴下提取失败即丢闭环，终审官会误判"伏笔悬空"
- 修复：新增 `apply_promise_closures(ledger_json, promise_results, chapter_idx)`——打勾 ✅ 条目直接标记 closed（recovered=章节号、evidence=校验证据句截 80 字）；映射关系为清单第 n 条 ↔ 台账 open 列表第 n 个（与 build_promise_block 同序）；verify_failed 标记与 ❌ 条目不联动；越界 id 安全
- 集成：generate_appended_chapter 打勾后、伏笔官提取前（伏笔官在最新台账上增删改，顺序一致）
- 验证四项单测 PASS（run10 真实打勾数据回归 5✅/1❌→closed 5、凭证落账、失败标记不联动、越界安全）+ 终审官视角验证（联动后【未收伏笔台账】只剩 1 条 open，5 条已闭不再出现在悬空清单）——终审官下次评审将不再误判"伏笔全悬空"
- 至此打勾机制完整闭环：必兑现清单注入 → 逐条打勾 → 未兑现补写 → ✅ 联动台账 → 终审官按真实台账评审

### 补充（2026-09-13）：无人值守四件套收官

1. **末章截断治理**：补全逻辑抽为公共函数 `ensure_complete_ending`（最多 2 轮补全+复验），主循环与结构补章共用（结构补章此前缺此防线）；截断判定标记单测 PASS
2. **Dart 端同步**：`lib/ai_pipeline/services/ai_pipeline_service.dart` 编辑采纳补齐 0.85 下限守卫（与 Python EDITOR_MIN_RATIO 同步），flutter analyze 通过
3. **端到端冒烟门禁**：`scripts/run_smoke_gate.py`——编译检查 → 3 章小样全链路 → 八项自动验收；口径设计要点：0 分评审=回归 FAIL（对应 max_tokens 静默失效 bug 形态）、低分章仅 WARN（定点修已尽力不是回归）；fulltest 产物干跑 8/8 PASS；支持 --check-only 只验收已有产物、断点续跑友好
4. **长跑守护**：`scripts/watchdog_novel.ps1`（UTF-8 BOM，PS5.1 语法 PASS）——60s 轮询、按命令行精确匹配不误杀其他 python、死了自动断点续跑、完成自动退出、锁文件防双守护；完成判定路径实测通过。用法见脚本头注释
- 至此「无人值守四件套」（截断治理/Dart 同步/冒烟门禁/watchdog）全部落地，距离可托付的 LLM 主导写书只差 ark 有效 key（可选第四弹药线）

### 补充（2026-09-13）：打勾机制推广到普通章节（选择性承诺）

- 策略：普通章不做全量回收（不现实），只把「埋设 ≥5 章未收」的超时伏笔升级为必兑现清单（每章最多 2 条硬约束）——只对快烂的伏笔上硬约束，普通章不背全部回收负担
- 实现：`build_promise_block` 加 `cur_idx`/`stale_after` 参数（普通章 stale 模式 / 收尾章全量模式共用）；主循环注入场景规划 state 与写手 scene_prompt（write_state_main）；生成后同款打勾→补写→✅ 联动台账；promise_check 落盘 + 章节 issues 附注
- 流程位置：主循环 3.6 钩子兜底后、4 标题前（正文完整后校验）
- 单测 3 项 PASS：stale 筛选（新鲜/超时/已收分流）、收尾章全量模式不受影响、无超时伏笔→空清单不上约束
- 代码位置：novel_pipeline.py build_promise_block 扩展 + 主循环 promise_block_main / write_state_main / promise_results_main
- 工程教训：bash heredoc 转义在本环境不可靠（\n 被实转换行导致语法错误），多行代码注入一律改用 Edit 工具

### 补充（2026-09-13）：投番茄质量差距诊断与两项精确优化

**诊断（fulltest《晏无咎》零成本离线解剖）**：
- 钩子判定：词表与终审官完全一致（非判定器问题）；人称全书零混用（幽灵名未发生）；💥直白爽点 0-1.06/千字偏低、✨变强异动充足（1.6-4.4）
- 字数：第 2/3/5 章过短（守卫修复前跑的书，下一本会被拦）

**根因一：钩子被后置修改吃掉（精确到行号取证）**
- 第 3 章时间线：229 行钩子兜底补写 → 237 行定点修改没 → 终审判"无钩"；第 2 章为 3.6 阶段词表判"有钩"→定点修后变"无钩"，均无复查
- 修复：新增 **7.55 钩子终检**——record 前最后一次校验，无钩再补一轮（覆盖定点修/首屏强化/评审修全部后置覆盖路径），补写记入 issues（type=hook_recheck）

**根因二：外显爽点执行衰减**
- fulltest 第 5 章规划"当众打脸"但💥命中 0.00——规划层目标正确、写手写成内心戏
- 修复：两处场景循环（主循环+结构补章）在场景 goal 含「外显爽点/打脸/当众」时注入硬约束："必须写出对手/旁观者当众外部反应（脸色骤变、失态、惊呼、修为显化、围观哗然），禁止只写主角内心感受"

**发现新角色**：并行会话已加第 12 角色「读者官」（READER_PROXY，上一章读者三问反馈注入下一章规划）——消费侧视角入闭环

### 补充（2026-09-16）：「不能投番茄」验证与四道质量闸

**验证（smoke_gate_v2 三章小样，终审官判 36 分不建议）暴露的不达标项**：
1. 补写模型的操作说明被当正文拼进第 1 章（「我拿到的指令是补写钩子，不是扩写…」），本地评审仍 92 分；
2. 第 3 章整章跑成现代都市悬疑（手机/面包车/路灯 34 处），玄幻书主线断裂；
3. 第 3 章开头 800 字整块重复两遍（复制粘贴级）；第 2 章无章末钩子；第 1 章仅 671 字；
4. 逐章评审 92/80/64 给「可投」，与终审 36 分「不建议」背离 50+ 分——口径分裂是根因；
5. 冒烟门禁自身崩溃（run_smoke_gate.py 把 f.count 用在文件对象上），验收环节失效；评估卡写「章节：0｜评审均分：0.0」空卡。

**修复（Python 与 Dart 双端同口径）**：
- fanqie_review.py：新增 META_TALK（元话语残留）、genre_drift（题材漂移，非现代题材才判定）、intra_repeat / dedup_intra_repeat（章内大段重复检测+自动去重）、patch_gate（补丁卫生：指令残留/题材漂移/红线/与正文尾部重复）、local_hook_fallback（章纲钩子零 LLM 兜底）、blocking_reasons（阻断级硬伤与扣分项分离）；review_chapter 有阻断项时 verdict 降为「需修」，review_book 有阻断章不给「可投」（实测：旧 81.3 分「需修」→ 新 78.7 分「不予推荐，3 章阻断」，与终审一致）；fix_prompt 有阻断项即使分数达标也出单，并加「题材一致/只输出正文」硬约束。
- novel_pipeline.py：钩子兜底三处、扩写、打勾、定点修两处、润色两处、首屏强化、续写、终审打回重写全部过 patch_gate / apply_text_patch；章末记录前跑 dedup_chapter；评审触发条件改 needs_fix（分数不达线或有阻断项）；评估卡 reviews 为空时按正文现算（治「章节：0」空卡）并写阻断项清单。
- run_smoke_gate.py：修 f.count 崩溃；验收从八项扩到十二项（元话语残留/章内重复/题材漂移/阻断章/评估卡非空卡）；新增 --genre。
- Dart 端 fanqie_gate_checker.dart：_metaTalk / genreDrift / intraRepeat / patchReject 与报告级 blockers；pass 与 fixPrompt 同步考虑阻断项；ai_pipeline_service.dart 钩子补写过闸 + 章纲钩子本地兜底。
- 测试：fanqie_gate_checker_test.dart 新增 6 个事故回归用例；全量 flutter test 667 过 / 0 挂；dart analyze 0 告警（2 条历史 info）。

### 补充（2026-09-13）：三项新优化 LLM 真书验证（smoke_gate_v2，极端限流环境）

**验证环境**：商汤 token plan entitlement exhausted（三 key 全耗尽）+ AMD 并发限制/502 + glm/dsf 思考链风暴——全天最恶劣窗口，机制验证反而最严格

**五项机制实战确认**：
1. **编辑守卫**：首次实战拦截（场景润色 261→175 字拒绝采纳保留原文）
2. **写前守门**：LLM 评审失败自动放行不阻塞（按设计降级）
3. **钩子终检**：首次实战触发——第 2 章定点修后钩子丢失被捕获，补出信息型钩子（"灰袍人认得他三年前掰断的铜钱——漕帮老帮主死前塞进他手里的信物"）
4. **多采样终审 + 区间判档 + 硬伤告警**：双采样 38/35→合并 36 分「不建议」，☠ 五维 ≤40 全部点名
5. **三级权力完整兑现**：终审官抓出两大事故（第 3 章题材/主角完全错位——玄幻变现代都市、季渊变林深；第 3 章大段正文重复）→ 打回全部 3 章重写 → **二审 36→68 分（+32）**——"发现问题→强制修复→分数回升"全链路首次真书实证

**重要运营结论**：配额耗尽时强行跑书，场景生成降级骨架兜底会导致题材错位/内容重复等事故——终审体系能全部拦截并驱动修复，但**质量产出需要配额正常窗口**。跑书前应探针确认配额，极端限流期只做机制验证不做质量验证。

**遗留**：门禁八项验收因限流未跑完，环境恢复后 `run_smoke_gate.py --check-only --output smoke_gate_v2.jsonl --max-chapters 3` 补验收即可。

### 补充（2026-09-13 晚）：干净窗口验证启动 + 写前守门首次拦截 + 查重盲区修复

- **写前守门首次真实拦截**：smoke_gate_v3 大纲被大纲终审官判 65 分不通过（4 个结构问题）→ 规划官修订版大纲被采纳落盘 → 生成继续。"烂大纲动笔前拦下"首次实证
- **查重盲区第三次实锤后修复**：v3 新大纲主角又撞"裴烬"（《焚骨逆天诀》主角，其 jsonl 在 scripts/ 目录）。`scan_used_protagonist_names` 扫描范围扩至 scripts/（验证书也算已用名），实测 11→15 名、裴烬入列
- v3 验证遇傍晚端点高峰（glm 死循环/dsf 7 连败/AMD 502），后台凭断点续传继续磨；两门禁并发注意：TRAE 工具也在并行操作本项目（解释此前文件外部更新）
- 待 v3 完成：八项门禁验收 + 与《晏无咎》76 分基线六维对比

## 十二、2026-09-23 语义硬伤检测器 v1（投前五关 · 前两关）

### 背景

2026-09-12 番茄老编辑人工试读结论：机器质检分抓不住四类语义级硬伤（人称漂移/角色名漂移/章节内容与标题不符/角色关系张冠李戴），《铁掌破风》机器 93.3 高潜🔥但人工复核四连翻车即为例证——投前把关全靠肉眼。本轮把其中前两类做成纯规则检测器（零 LLM 成本），作为「投前五关」第一批关卡。

### 新增 scripts/qa_semantic_check.py（独立 CLI，函数可导入复用）

**关卡 1 · 叙述人称一致性**：块级（默认 500 字）剔引号取叙述层 → 第一/第三人称代词占比判定 → 游程编码（稳定段 >=2 块）→ 相邻稳定段人称不同即报切换事件。主导判定须 >=3 块且过半，防独白密集章误标。

**关卡 2 · 主角称谓一致性**（三条信号）：
- S1 占位称呼（X某/姓X的/X小子…，簇级合计 >=2 才提示——单次路人群像无报警价值）
- S2 主角级簇区间分析：按姓聚簇，真实首现~末现区间，两两重叠率 <0.15 报警；仅对章覆盖率 >=0.3 且有高频代表名（>=5 次）的簇参与——配角区间窄是正常现象
- S2· 接管信号：占位称呼消失后 top1 高频簇才首现 → 高置信主角换名（正对《铁掌破风》事故形态）

### 校准过程（真实成书回测 → 修阈值 → 复测，6 本书）

- 正样本《铁掌破风》：两关全命中（第 1 章 75% 处 first→third 切换 + 苏系占位称呼 7% 消失后裴簇 40% 接管）
- 负样本 5 本全零警报：《碎脉铸仙录》(10.6w字)/《铁幕孤刃》/《断针开井》/《残剑惊鸿》/《断脉逆命诀》
- 校准中修掉的误报源：POV_MIN_PRON 8→5（漏掉短第三人称块）；称谓/亲属/身体部位/器物/方位第二字黑名单（师尊/师父/石头/左肩/柴刀/井底…）；姓氏表补网文常用姓（裴/晏/郗/霍…）+ 移除高频撞词姓（都/连/步/边/温/红）；懒惰+回溯子串名归并（裴照没→裴照）；区间分析改用真实末现 offset（此前只有首现，王麻子贯穿全书却显示区间 [0.16,0.16]）

### 后续

- 关卡 3（标题-内容相符）：✅ 2026-09-23 已落地（见下）
- 关卡 4（关系归属）：✅ 2026-09-23 已落地（见第十三节）——防（状态清单入关系）+ 检（detect_relation_drift 纯规则）+ 门禁第 14 项
- 关卡 5（编造核查）：✅ 2026-09-23 已落地（见第十三节）——规划官 glm 链（与写手异家族）对照户籍表/数字台账/状态清单核查，type=fact_check 落盘 + 门禁第 15 项
- 五关已全部落地并入冒烟门禁 run_smoke_gate（第 13~15 项），作为导出投递版前置条件

### 关卡 3 补充（同日）：题材内容塌陷 + 标题词命中（detect_content_drift）

- 词表定位与 GENRE_THRILL_EXTRA（爽点词）不同：自建 `GENRE_CONTENT_WORDS`（11 题材 × 场景元素词），判"章节内容塌陷"
- 判定：题材词零命中（且 >=800 字）→ 塌陷（组合"标题词未现"升级高置信）；密度 < 全书中位数 ×15% → 提示级。词表用途是**同书内章间对比**（跑偏章 vs 全书中位数），故按"宁全勿缺"收土味变体（铜锁/锁芯/断剑/短刀…）
- 验证：正样本《铁掌破风》--genre 体育 两章全报塌陷（0 命中，标题词"废掌/试训"正文未现——完整复现人工试读"跑偏成守井人玄学"结论）；负样本 5 本（玄幻/军事/科幻/武侠）零误报（首轮科幻/武侠因词表偏"星际/门派风"误报，补废土系与单字"剑/刀"后清零）
- 自动探测：--genre 缺省时按各题材章密度中位数最高者自动选题材

### 门禁集成（同日）：run_smoke_gate.py 第 13 项「语义三关无硬伤」

- 冒烟门禁验收从 12 项扩到 13 项：`check()` 对 jsonl 章节数据直接调用 qa_semantic_check 的三个检测函数（同口径，import 复用不复制逻辑）
- 拦截口径：人称切换 / 主角级称谓区间断裂 / 主角换名接管 / 题材内容塌陷（high/medium）→ FAIL；占位称呼（S1）与题材密度偏低（suspect）→ 仅 WARN
- span 分析收紧为「频次 top2 才参与两两比较」：两个小配角区间不重叠是正常现象，3 章小样配角覆盖 1/3 章也能过 coverage≥0.3 门槛，不收紧会误报
- 首战即中：对 smoke_gate_v3.jsonl（恶劣配额窗口的历史事故产物）跑 --check-only，第 13 项精准拦下两处真伤——①陈默(36次,第3章)/周成(28次,第1章)两章两个主角名（即当次"主角完全错位"事故）②第 2 章玄幻词 0 命中（正文实为"运费/算盘"现代都市内容，即"题材错位"事故）——验证门禁能拦住历史上真实发生过的生成事故形态
- 取舍记录：不加"跨章人称不一致"信号——碎脉铸仙录等第三人称书的独白密集章（first 块多）会造成大规模误报；整章人称漂移通常伴随题材跑偏，由关卡 3 题材塌陷兜底
- 回归：五本负样本零警报不变、《铁掌破风》正样本 4 处警报不变

## 十三、2026-09-23 关卡 4/5 落地 + 户籍表接入主流水线 + 门禁十五项

### 关卡 4 · 人物关系归属（防 + 检 + 门禁）

- **防（双端）**：状态提取 prompt 硬状态清单新增「人物关系」条目（Python `novel_pipeline.state_extract_prompt` + Dart `pipeline_prompts.stateExtractPrompt`），格式行同步「关系：A是B的师父」——关系入跨章状态清单随章注入，确立后不得改写换人
- **检（纯规则零成本）**：`qa_semantic_check.detect_relation_drift`——排他型关系词 11 个（父亲/母亲/师父/师傅/丈夫/妻子/夫人/儿子/女儿/义父/义母；徒弟/师兄/朋友/对手等天然多值词不参与），双向句式归一为 (关系, 被修饰者)→对象：P1「林舟的父亲是林啸天」/ P2「林啸天是林舟的父亲」同键
- **防误报三件套**（负样本零误报优先，宁可漏报）：① 名字含代词/否定/量词/结构字整条丢弃（「他是林啸天的父亲」「母亲是一位医生」类）② 时地/评注副词前后缀循环剥离归一（当年林啸天 ≡ 林啸天 ≡ 林啸天就是）③ P1 对象侧 `(?![汉字])` 边界——后接叙述文字的贪婪吞字直接不匹配
- **rel 捕获组**：rel 为正则独立捕获组（非整句 next() 扫描），防「王夫人的女儿」这类被修饰者姓名含关系词时取错
- **判定**：同章同键两对象 → high（章内自相矛盾）；跨章指向不同 → medium（身世揭露类反转可能触发，人工复核）
- **校准**：合成断言 9/9 PASS（双向一致零警报/跨章 medium/同章 high/对话剔除/代词量词过滤/副词归一/rel 捕获/关卡5 prompt 渲染/状态关系条目）；8 本真书（正样本《铁掌破风》+ 7 本负样本含 v3 事故产物）**零误报**——真书语料用领属表述（「师父让他扫台阶」）不用判断句，claims=0 属语料形态而非漏检，规则层口径与关卡 1-3 一致：宁可漏报不可误报
- **门禁**：`run_smoke_gate` 第 14 项，high/medium 均 FAIL

### 关卡 5 · 编造核查（LLM 换模型家族独立核查）

- `novel_pipeline` 新增 `FACT_CHECK_SYS` + `fabrication_check_prompt` + `run_fact_check`：对照**户籍表/数字台账/跨章状态清单**核查四类编造——身份矛盾（户籍表内姓名换身份）/ 数字漂移（同一事项金额期限与台账冲突；新事项首次数字不算）/ 状态矛盾（伤势修为物品得失冲突）/ 无中生有（把前文未确立细节当前文既定事实）
- **异家族**：核查走规划官 glm 链（写手主位 dsf-flash/DeepSeek-V4 异家族，防同源错误），max_tokens=3000，解析失败/异常返回 None
- **时序硬约束**：必须在户籍表 `update_registry` **前**调用——核查的是「本章 vs 前章已确立事实」（本章新确立的数字先进台账会自我豁免）
- **落盘** `type=fact_check {idx, fabrications, error}`：失败记 error **不算通过凭据**（与「评审 0 分=静默失效」同口径）；编造写入 chapter issues（type=fabrication）随 record 落盘；每章最多打印 8 条
- **开关**：`--no-fact-check`（默认开启）；主循环 + 结构补章 `generate_appended_chapter` 双路径接入
- **门禁第 15 项**：有记录且存在编造/执行失败 → FAIL；无记录（历史产物或开关关闭）→ WARN 降级不计入拦截

### 户籍表/数字台账接入主流水线（关卡 4/5 的事实源，此前是死代码）

- 现状修复：`generate_novel` 单模型 CLI 已完整接线，但 `novel_pipeline` 主流水线**零引用**——`registry_block/facts_block/extract_registry_prompt/merge_registry/merge_facts` 定义了却没人调；场景规划/写手 prompt 的 registry/facts 参数恒传空串
- 接线四处：
  1. **断点恢复**：`load_state` 本就回放最后一条 type=registry（last-wins），主循环 init `registry = state.get("registry") or {...}` + [RESUME] 打印
  2. **注入**：主循环与结构补章两处 `scene_planning_prompt` + 两处 `scene_prompt` 均传 `registry=registry_block(registry), facts=facts_block(registry)`
  3. **更新**：每章定稿后 `update_registry`——数字本地 `merge_facts` 首见即锁定（零成本）+ 身份 dsf-flash LLM 提取（失败保留旧表，冲突只记警示「疑似穿帮」）→ type=registry 落盘；主循环在伏笔提取后、结构补章在 record 后
  4. **ctx 传引用**：`ctx["registry"] = registry`，结构补章与主循环共享同一对象，收尾章户籍入册
- 时序：fact-check（章定稿后、record 前）→ record → 状态/伏笔提取 → update_registry → 下一章注入

### 验证

- py_compile 4 文件 + ruff --select=F 全绿；`novel_pipeline --help` 显示 `--no-fact-check`
- 合成断言 9/9 PASS；8 本真书关系关零误报
- 门禁干跑 smoke_gate_v3：第 14 项 PASS、第 15 项 WARN 降级（历史产物无记录）——总 11/14 项计入，存量事故仍被正确拦 FAIL
- Dart：`dart analyze --fatal-infos` 零告警；`pipeline_prompts_test.dart` 新增 4 用例锁定双端同步（关系条目/证据块注入与不注入）

### 门禁项数演进

十二（2026-09-16）→ 十三（语义三关，09-23）→ **十五**（+ 关系张冠李戴第 14 项 + 编造核查记录第 15 项；无 fact_check 记录时 14 项计入 + 1 项 WARN）
