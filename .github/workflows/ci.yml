# 墨匠 InkSmith — CI 流水线
# 触发: push / PR 到 main、master；也可手动 workflow_dispatch
# 步骤: flutter analyze → flutter test → web 工作台浏览器回归 → (Windows) Release 构建

name: CI

# 同一分支有新推送时，取消仍在跑的旧 CI（省额度、更快反馈）
concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]
  workflow_dispatch:

jobs:
  analyze-test:
    name: Analyze + Test
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v5

      - name: Python scripts quality check
        # scripts/ Python 流水线必须通过：语法编译 + ruff F 类静态检查
        # （F 类含未定义名/未使用导入等运行时级 bug，compileall 抓不到；
        # 历史教训：StateError 错用就是纯语法检查漏掉的 NameError）。
        run: |
          python3 -m compileall -q scripts
          python3 -m pip install --quiet ruff
          # 只对「已入库的脚本」做 F 类检查：临时探针/诊断脚本不入库也不设防
          git ls-files 'scripts/*.py' | xargs -r python3 -m ruff check --select=F --output-format=concise

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          # 与开发环境/CI 双端一致：代码使用 onReorderItem（3.41+）、
          # Color.withValues（3.27+）等新 API，3.24 无法编译。
          flutter-version: '3.44.9'
          channel: stable
          cache: true

      - name: Enable Flutter web (analyze 需要)
        run: flutter config --enable-web

      - name: Pub get
        # CI 网络瞬断（pub.dev 偶发 5xx / 连接重置）首跑实测踩过：单次调用
        # 直接挂掉整个 job（exit 65）。加 3 次重试 + 注解留痕，匿名也能在
        # Actions summary 的 Annotations 里看到重试原因。
        run: |
          for i in 1 2 3; do
            if flutter pub get --enforce-lockfile; then exit 0; fi
            echo "::notice::flutter pub get attempt ${i} failed (network or resolution), retrying in 20s"
            sleep 20
          done
          echo "::error::flutter pub get failed after 3 attempts"
          exit 1

      - name: Offline demo write smoke
        # 零 API 费端到端回归：模板引擎连写三章 + 番茄双质检（NovelQualityChecker
        # + FanqieGateChecker）必须产出非空正文并返回成功。结果会写入
        # verify-logs/（gitignore），不污染工作区。
        run: dart run tool/write_demo_novel.dart

      - name: Dart analyze
        # --fatal-infos：任何 info（含 deprecation）都算失败，兑现 README「零告警」承诺
        run: dart analyze --fatal-infos

      - name: Flutter test
        run: flutter test --coverage

      - name: Enforce coverage threshold
        # 覆盖率低于阈值时失败，防止测试退化
        run: |
          COV=$(grep -oP 'LF:\d+' coverage/lcov.info | awk -F: '{s+=$2} END {print s}')
          LH=$(grep -oP 'LH:\d+' coverage/lcov.info | awk -F: '{s+=$2} END {print s}')
          echo "Lines hit: $LH / $COV"
          PERCENT=$(python3 -c "print(round(100.0*$LH/$COV, 1))" 2>/dev/null || echo "unknown")
          echo "Coverage: $PERCENT%"
          if [ "$PERCENT" != "unknown" ] && [ "$(python3 -c "print(1 if $PERCENT >= 80 else 0)")" = "0" ]; then
            echo "::error::Coverage $PERCENT% below threshold 80%"
            exit 1
          fi

      - name: Upload coverage
        uses: actions/upload-artifact@v4
        with:
          name: coverage
          path: coverage/lcov.info
          retention-days: 7

  verify-web:
    name: Web workbench verify
    # 网页工作台（web/index.html + app.js + ai.js）的真实浏览器回归：
    # 书架 CRUD / 章节编辑 / AI 守卫 / 导入原子性 / 无运行时异常等。
    # 与 Flutter 任务无依赖，可并行；Chrome 由 ubuntu-latest 镜像预装。
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v5

      - name: Setup Node
        uses: actions/setup-node@v5
        with:
          node-version: '22'

      - name: Ensure Chrome available
        # runner 镜像不保证预装 google-chrome（首跑 8 秒秒挂的根因疑点）：
        # 优先用镜像自带的，没有就用 @puppeteer/browsers 装一份稳定版。
        run: |
          if command -v google-chrome >/dev/null 2>&1; then
            echo "Chrome found: $(google-chrome --version)"
            echo "CHROME_PATH=$(command -v google-chrome)" >> "$GITHUB_ENV"
          else
            echo "::notice::google-chrome not preinstalled on runner image, installing via @puppeteer/browsers"
            npx -y @puppeteer/browsers install chrome@stable --path "$RUNNER_TEMP/chrome"
            BIN=$(find "$RUNNER_TEMP/chrome" -type f -name chrome | head -n 1)
            if [ -z "$BIN" ]; then echo "::error::Chrome install finished but binary not found"; exit 1; fi
            echo "Installed Chrome at: $BIN"
            echo "CHROME_PATH=$BIN" >> "$GITHUB_ENV"
          fi

      - name: Browser regression (3 suites)
        run: |
          node scripts/verify_web.cjs
          node scripts/verify_ai_failure.cjs
          node scripts/verify_web_ai.cjs

  build-windows:
    name: Build Windows Release
    needs: analyze-test
    runs-on: windows-latest
    timeout-minutes: 40
    steps:
      - uses: actions/checkout@v4

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.44.9'
          channel: stable
          cache: true

      - name: Pub get
        run: flutter pub get --enforce-lockfile

      - name: Build Windows Release
        run: flutter build windows --release

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: novel-writer-windows
          path: build/windows/x64/runner/Release/
          retention-days: 14
